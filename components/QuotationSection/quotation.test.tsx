import { render, screen } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import Quotation from "./Quotation";

describe("<Quotation/>", () => {
  it("should show quotation section", async () => {
    // render(<Quotation />);
    throw Error("Not implemented");
  });
});
