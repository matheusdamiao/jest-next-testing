import React from "react";
import style from "./hero.module.css";

const Hero = () => {
  return (
    <div data-testid="heroSection" className={style.heading}>
      {" "}
      testando
    </div>
  );
};

export default Hero;
